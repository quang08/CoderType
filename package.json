{
  "name": "codertype",
  "version": "1.0.0",
  "description": "A speed typing game for real coders",
  "author": "quang",
  "license": "MIT",
  "source": "index.html",
  "scripts": {
    "start": "parcel",
    "clean": "rm -rf dist",
    "build": "yarn clean && parcel build",
    "deploy": "yarn build && firebase deploy --only hosting:coder-type"
  },
  "devDependencies": {
    "firebase-tools": "^11.24.1",
    "parcel": "^2.8.3"
  },
  "dependencies": {
    "firebase": "^9.18.0"
  }
}
